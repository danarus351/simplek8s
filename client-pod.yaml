apiVersion : v1
kind: Pod
metadata:
    name: client-Pod
    lables:
        components: web
spec:
    containers:
        -name: client
        image: danarus/multi_client
        ports:
            -containerPort: 3000